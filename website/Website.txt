Combined HTML - 23/01/2026
 
=== afohtml === 
<DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AFO Mapping Generator</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    :root {
      --primary-color: #ff4757; 
      --secondary-color: #ffffff;
      --background-color: #0a0a0a;
      --text-color: #ffffff;
      --hover-color: #ff6b81;
      --card-bg: rgba(0, 0, 0, 0.6);
      --input-bg: rgba(255, 255, 255, 0.05);
      --text-light: #dfe6e9;
    }
    * { box-sizing: border-box; margin: 0; padding: 0; scrollbar-width: thin; scrollbar-color: var(--primary-color) rgba(0,0,0,0.3); }
    ::-webkit-scrollbar { width: 12px; }
    ::-webkit-scrollbar-track { background: rgba(0,0,0,0.3); }
    ::-webkit-scrollbar-thumb { background: var(--primary-color); border-radius: 6px; border: 2px solid rgba(0,0,0,0.3); }
    body { font-family: 'Poppins', sans-serif; color: var(--text-color); background: var(--background-color); overflow-x: hidden; line-height: 1.6; }
    .loading-screen { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: #000; display: flex; flex-direction: column; justify-content: center; align-items: center; z-index: 9999; transition: opacity 0.8s; }
    .loading-screen.hidden { opacity: 0; visibility: hidden; }
    .loading-bar-container { width: 300px; height: 4px; background: #333; margin-top: 20px; border-radius: 2px; }
    .loading-bar { width: 0%; height: 100%; background: var(--primary-color); transition: width 0.5s; }
    .bg-video { position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -1; opacity: 0.3; object-fit: cover; }
    .container { max-width: 1100px; margin: 0 auto; padding: 2rem; min-height: 100vh; display: flex; flex-direction: column; }
ECHO is off.
    .main-content { background: var(--card-bg); border-radius: 16px; padding: 3rem; backdrop-filter: blur(12px); border: 1px solid rgba(255,255,255,0.1); animation: fadeIn 1s ease-out; }
    @keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
    h1 { font-size: 2.5rem; text-shadow: 0 2px 10px rgba(255, 71, 87, 0.5); }
    h2 { font-size: 1.5rem; color: var(--primary-color); margin: 2rem 0 1rem; border-bottom: 1px solid rgba(255,255,255,0.1); display: inline-block; padding-bottom: 5px; }
    .form-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1.5rem; }
    .input-group { display: flex; flex-direction: column; gap: 0.5rem; }
    label { font-size: 0.85rem; color: var(--text-light); }
    input, select { background: var(--input-bg); border: 1px solid rgba(255,255,255,0.1); color: white; padding: 0.8rem; border-radius: 8px; font-family: inherit; width: 100%; }
    input:focus { outline: none; border-color: var(--primary-color); box-shadow: 0 0 15px rgba(255, 71, 87, 0.2); }
    .controls { margin-top: 2rem; display: flex; gap: 1rem; flex-wrap: wrap; }
    .btn { background: var(--primary-color); color: white; padding: 1rem 2rem; border-radius: 50px; text-decoration: none; font-weight: 600; cursor: pointer; border: none; transition: 0.3s; }
    .btn:hover { transform: translateY(-3px); box-shadow: 0 5px 15px rgba(255, 71, 87, 0.4); }
    .btn-download { background: #00cec9; }
    .item-list { display: flex; flex-wrap: wrap; gap: 1rem; margin-top: 2rem; }
    .item-card { background: rgba(255,255,255,0.05); padding: 1rem 1.5rem; border-radius: 12px; border: 1px solid rgba(255,255,255,0.1); display: flex; align-items: center; gap: 1rem; min-width: 250px; }
    .tech-icon { font-size: 1.2rem; color: var(--primary-color); background: rgba(255,255,255,0.1); padding: 10px; border-radius: 50%; }
  </style>
</head>
<body>
  <div id="loadingScreen" class="loading-screen">
    <h1 style="color:white;">All For One</h1>
    <div class="loading-bar-container"><div id="loadingBar" class="loading-bar"></div></div>
  </div>
  <video class="bg-video" autoplay muted loop><source src="images/background.mp4" type="video/mp4"></video>
  <div class="container">
    <main class="main-content">
      <h1>AFO Mapping Generator</h1>
      <p style="color:#aaa; margin-bottom:2rem;">Map techniques, skills, and mob associations for the All For One system.</p>
      <form id="afoForm">
        <h2>Technique Details</h2>
        <div class="form-grid">
          <div class="input-group">
            <label>Technique ID (Integer)</label>
            <input type="number" id="id" placeholder="e.g. 1001" required>
          </div>
          <div class="input-group">
            <label>Display Name</label>
            <input type="text" id="name" placeholder="e.g. Blood Manipulation">
          </div>
          <div class="input-group">
            <label>Icon Item ID</label>
            <input type="text" id="icon" placeholder="minecraft:redstone">
          </div>
          <div class="input-group">
            <label>AI Profile (Optional)</label>
            <input type="text" id="ai_profile" placeholder="e.g. choso">
          </div>
        </div>
        <h2>Configuration</h2>
        <div class="form-grid">
          <div class="input-group">
            <label>Skill IDs (Comma separated)</label>
            <input type="text" id="skills" placeholder="1005, 1006, 1020">
          </div>
          <div class="input-group">
            <label>Mob IDs (Comma separated)</label>
            <input type="text" id="mobs" placeholder="jujutsucraft:choso, jujutsucraft:kamo">
          </div>
        </div>
        <div class="controls">
          <button type="button" class="btn" onclick="addTechnique()">
            <i class="fas fa-hand-holding-magic"></i> Add Mapping
          </button>
        </div>
      </form>
      <div id="itemList" class="item-list">
        <span style="color:rgba(255,255,255,0.3);">No mappings added yet.</span>
      </div>
      <div class="controls" style="border-top: 1px solid rgba(255,255,255,0.1); padding-top: 2rem;">
        <div class="input-group" style="max-width:300px;">
          <label>File Name</label>
          <input type="text" id="file_name" value="custom_afo_mappings">
        </div>
        <button type="button" class="btn btn-download" onclick="downloadPack()">Download Datapack</button>
      </div>
    </main>
  </div>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/FileSaver.js/2.0.5/FileSaver.min.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      let width = 0;
      const interval = setInterval(() => {
        width += 20; document.getElementById('loadingBar').style.width = width + '%';
        if(width >= 100) { clearInterval(interval); setTimeout(() => document.getElementById('loadingScreen').classList.add('hidden'), 500); }
      }, 150);
    });
    let techniques = [];
    function val(id, type = 'string') {
      const el = document.getElementById(id);
      if(type === 'int') return parseInt(el.value) || 0;
      if(type === 'list_int') return el.value ? el.value.split(',').map(s => parseInt(s.trim())).filter(n =>  [];
      if(type === 'list_str') return el.value ? el.value.split(',').map(s => s.trim()).filter(s => s) : [];
      return el.value;
    }
    function addTechnique() {
      const id = val('id', 'int');
      if(id) return alert("Technique ID is required");
      const tech = {
        id: id,
        name: val('name'),
        icon: val('icon'),
        ai_profile: val('ai_profile') || null,
        skills: val('skills', 'list_int'),
        mob_associations: val('mobs', 'list_str')
      };
      if(tech.skills.length === 0) delete tech.skills;
      if(tech.mob_associations.length === 0) delete tech.mob_associations;
      techniques.push(tech);
ECHO is off.
      const list = document.getElementById('itemList');
      if(techniques.length === 1) list.innerHTML = '';
      list.innerHTML += `
        <div class="item-card">
          <i class="fas fa-skull tech-icon"></i>
          <div><strong>${tech.name}</strong><br><span style="font-size:0.8em; opacity:0.7;">ID: ${tech.id}</span></div>
        </div>
      `;
      document.getElementById('id').value = "";
      document.getElementById('name').value = "";
      document.getElementById('skills').value = "";
      document.getElementById('mobs').value = "";
    }
    function downloadPack() {
      if(techniques.length === 0) return alert("Add at least one technique");
ECHO is off.
      const zip = new JSZip();
      zip.file("pack.mcmeta", JSON.stringify({ pack: { pack_format: 15, description: "Omega AFO Mappings" } }, null, 2));
ECHO is off.
      const finalJson = { techniques: techniques };
ECHO is off.
      const filename = document.getElementById('file_name').value || "afo_mappings";
      const folder = zip.folder("data").folder("omega").folder("afo_mappings");
ECHO is off.
      folder.file(`${filename}.json`, JSON.stringify(finalJson, null, 2));
      zip.generateAsync({type:"blob"}).then(c => saveAs(c, "omega_afo_datapack.zip"));
    }
  </script>
</body>
</html>
 
=== clanshtml === 
<DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Omega Clan Generator</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    :root {
      --primary-color: #6c5ce7;
      --secondary-color: #ffffff;
      --background-color: #0a0a0a;
      --text-color: #ffffff;
      --hover-color: #00cec9;
      --card-bg: rgba(0, 0, 0, 0.6);
      --input-bg: rgba(255, 255, 255, 0.05);
      --text-light: #dfe6e9;
    }
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      scrollbar-width: thin;
      scrollbar-color: var(--primary-color) rgba(0, 0, 0, 0.3);
    }
    ::-webkit-scrollbar { width: 12px; }
    ::-webkit-scrollbar-track { background: rgba(0, 0, 0, 0.3); border-radius: 6px; }
    ::-webkit-scrollbar-thumb {
      background: linear-gradient(45deg, var(--primary-color), var(--hover-color));
      border-radius: 6px;
      border: 2px solid rgba(0, 0, 0, 0.3);
    }
    body {
      font-family: 'Poppins', sans-serif;
      color: var(--text-color);
      margin: 0;
      background: var(--background-color);
      overflow-x: hidden;
      line-height: 1.6;
    }
    .loading-screen {
      position: fixed;
      top: 0; left: 0; width: 100%; height: 100%;
      background: linear-gradient(135deg, #000000 0%, #0a0a0a 100%);
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      z-index: 9999;
      transition: opacity 0.8s ease-out, visibility 0.8s ease-out;
    }
    .loading-screen.hidden { opacity: 0; visibility: hidden; }
ECHO is off.
    .loading-logo {
      font-size: 4rem; font-weight: 700; margin-bottom: 2rem;
      text-shadow: 0 4px 8px rgba(108, 92, 231, 0.3);
      animation: logoFloat 3s ease-in-out infinite;
    }
    @keyframes logoFloat { 0%, 100% { transform: translateY(0px); } 50% { transform: translateY(-10px); } }
    .loading-bar-container {
      width: 300px; height: 6px; background: rgba(255,255,255,0.1);
      border-radius: 3px; overflow: hidden; position: relative;
    }
    .loading-bar {
      height: 100%; background: #6c5ce7; width: 0%;
      transition: width 0.3s ease;
      box-shadow: 0 0 10px rgba(108, 92, 231, 0.5);
    }
    .bg-video {
      position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -1;
      background: linear-gradient(135deg, #111 0%, #222 100%);
    }
    .bg-video video {
      min-width: 100%; min-height: 100%; width: auto; height: auto;
      position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);
      object-fit: cover; opacity: 0.4;
    }
    .container {
      max-width: 1100px; margin: 0 auto; padding: 2rem;
      display: flex; flex-direction: column; min-height: 100vh;
    }
    header { display: flex; justify-content: space-between; align-items: center; padding: 1rem 0; }
    .logo { font-size: 1.8rem; font-weight: 700; color: var(--text-color); text-decoration: none; }
    .logo span { color: var(--primary-color); }
    .main-content {
      background-color: var(--card-bg);
      border-radius: 16px; padding: 3rem;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4);
      backdrop-filter: blur(12px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      animation: fadeIn 1s ease-out;
    }
    @keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
    h1 { font-size: 2.5rem; margin-bottom: 0.5rem; text-shadow: 0 2px 4px rgba(0,0,0,0.5); }
    h2 { font-size: 1.5rem; color: var(--hover-color); margin: 2rem 0 1rem; border-bottom: 2px solid var(--primary-color); display: inline-block; }
    p.intro { color: var(--text-light); margin-bottom: 2rem; }
    .form-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1.5rem;
      margin-bottom: 1.5rem;
    }
    .input-group { display: flex; flex-direction: column; gap: 0.5rem; }
ECHO is off.
    label { font-size: 0.9rem; color: var(--text-light); font-weight: 500; }
ECHO is off.
    input, select {
      background: var(--input-bg);
      border: 1px solid rgba(255, 255, 255, 0.1);
      color: white;
      padding: 0.8rem;
      border-radius: 8px;
      font-family: 'Poppins', sans-serif;
      transition: all 0.3s ease;
    }
ECHO is off.
    input:focus, select:focus {
      outline: none;
      border-color: var(--primary-color);
      box-shadow: 0 0 10px rgba(108, 92, 231, 0.3);
      background: rgba(255, 255, 255, 0.1);
    }
    input[type="color"] { height: 45px; width: 100%; cursor: pointer; padding: 0 5px; }
    .clan-list {
      display: flex; flex-wrap: wrap; gap: 1rem; margin: 1rem 0 2rem;
    }
    .clan-card {
      background: rgba(255, 255, 255, 0.05);
      padding: 0.8rem 1.5rem;
      border-radius: 50px;
      border: 1px solid rgba(255, 255, 255, 0.2);
      display: flex; align-items: center; gap: 0.8rem;
      animation: popIn 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    }
    .clan-dot { width: 12px; height: 12px; border-radius: 50%; box-shadow: 0 0 5px currentColor; }
    @keyframes popIn { from { transform: scale(0.5); opacity: 0; } to { transform: scale(1); opacity: 1; } }
    .controls { display: flex; gap: 1rem; flex-wrap: wrap; margin-top: 2rem; }
ECHO is off.
    .btn {
      display: inline-flex; align-items: center; gap: 0.5rem;
      color: white; background-color: var(--primary-color);
      padding: 1rem 2rem; border-radius: 50px;
      text-decoration: none; font-weight: 600; cursor: pointer;
      border: 1px solid rgba(255,255,255,0.1);
      transition: all 0.3s ease;
    }
    .btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 20px rgba(108, 92, 231, 0.4);
      background-color: var(--hover-color);
    }
ECHO is off.
    .btn-add { background: linear-gradient(45deg, #6c5ce7, #a29bfe); }
    .btn-download { background: linear-gradient(45deg, #00b894, #00cec9); }
    .btn-clear { background: transparent; border: 2px solid #ff7675; color: #ff7675; }
    .btn-clear:hover { background: #ff7675; color: white; }
    footer {
      text-align: center; margin-top: auto; padding: 2rem 0;
      color: rgba(255,255,255,0.5); font-size: 0.9rem;
    }
  </style>
</head>
<body>
  <div id="loadingScreen" class="loading-screen">
    <div class="loading-logo">Omega Gen</div>
    <div class="loading-bar-container">
      <div id="loadingBar" class="loading-bar"></div>
    </div>
    <div style="margin-top: 10px; color: #888;">Initializing Datapack Engine...</div>
  </div>
  <div class="bg-video">
    <video id="bgVideo" autoplay muted loop>
      <source src="images/background.mp4" type="video/mp4">
    </video>
  </div>
  <div class="container">
    <header>
      <a href="#" class="logo">Slyrien's <span>Omega</span></a>
    </header>
    <main class="main-content">
      <h1>Clan Generator</h1>
      <p class="intro">Design custom clans for the Omega mod. Configure traits, stats, and Jujutsu multipliers, then download a ready-to-use datapack.</p>
      <form id="clanForm">
ECHO is off.
        <h2>Identity</h2>
        <div class="form-grid">
          <div class="input-group">
            <label>Clan ID (e.g. zenin)</label>
            <input type="text" id="id" placeholder="Technical name (lowercase)" required>
          </div>
          <div class="input-group">
            <label>Display Name</label>
            <input type="text" id="display_name" placeholder="In-game Name">
          </div>
          <div class="input-group">
            <label>Clan Color</label>
            <input type="color" id="color" value="#6c5ce7">
          </div>
          <div class="input-group">
            <label>Has God Ki?</label>
            <select id="has_god_ki">
              <option value="false">No</option>
              <option value="true">Yes</option>
            </select>
          </div>
        </div>
        <h2>Features</h2>
        <div class="form-grid">
          <div class="input-group">
            <label>Specializations (IDs)</label>
            <input type="text" id="specializations" placeholder="Sage, Cataclysm">
          </div>
          <div class="input-group">
            <label>Default Traits</label>
            <input type="text" id="default_traits" placeholder="Angel, Fallen">
          </div>
          <div class="input-group">
            <label>Default Physical Traits</label>
            <input type="text" id="default_physical_traits" placeholder="Vessel">
          </div>
        </div>
        <h2>Technique Multipliers (1.0 = Normal)</h2>
        <div class="form-grid">
          <div class="input-group"><label>Damage Mult</label><input type="number" id="damage_multiplier" value="1.0" step="0.1"></div>
          <div class="input-group"><label>Tech Cost Mult</label><input type="number" id="technique_cost_mult" value="1.0" step="0.1"></div>
          <div class="input-group"><label>Domain Expansion</label><input type="number" id="domain_expansion_mult" value="1.0" step="0.1"></div>
          <div class="input-group"><label>Simple Domain Radius</label><input type="number" id="simple_domain_radius_mult" value="1.0" step="0.1"></div>
          <div class="input-group"><label>RCT Efficiency</label><input type="number" id="rct_efficiency_mult" value="1.0" step="0.1"></div>
          <div class="input-group"><label>RCT Fatigue</label><input type="number" id="rct_fatigue_mult" value="1.0" step="0.1"></div>
          <div class="input-group"><label>DA Drain</label><input type="number" id="da_drain_mult" value="1.0" step="0.1"></div>
          <div class="input-group"><label>Black Flash Bonus</label><input type="number" id="black_flash_chance_bonus" value="0.0" step="0.01"></div>
        </div>
        <h2>Base Stat Bonuses</h2>
        <div class="form-grid">
          <div class="input-group"><label>Bonus Health</label><input type="number" id="bonus_health" value="0"></div>
          <div class="input-group"><label>Bonus Armor</label><input type="number" id="bonus_armor" value="0"></div>
          <div class="input-group"><label>Bonus Damage</label><input type="number" id="bonus_attack_damage" value="0"></div>
          <div class="input-group"><label>Bonus Speed</label><input type="number" id="bonus_speed" value="0" step="0.01"></div>
        </div>
        <div class="controls">
          <button type="button" class="btn btn-add" onclick="addClan()">
            <i class="fas fa-plus"></i> Add Clan
          </button>
        </div>
      </form>
      <h2>Current Pack Contents</h2>
      <div id="clanList" class="clan-list">
        <span style="color:rgba(255,255,255,0.3); font-style: italic;">No clans added yet. Add one above</span>
      </div>
      <div class="controls" style="border-top: 1px solid rgba(255,255,255,0.1); padding-top: 2rem;">
        <div class="input-group" style="max-width: 300px;">
          <label>Datapack Name</label>
          <input type="text" id="pack_desc" value="Omega Custom Clans">
        </div>
        <button type="button" class="btn btn-download" onclick="downloadPack()">
          <i class="fas fa-download"></i> Download Datapack
        </button>
        <button type="button" class="btn btn-clear" onclick="clearList()">
          <i class="fas fa-trash"></i> Clear
        </button>
      </div>
    </main>
    <footer>
      <p>&copy; <span id="year"></span> Slyrien's Omega Utilities</p>
    </footer>
  </div>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/FileSaver.js/2.0.5/FileSaver.min.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const bar = document.getElementById('loadingBar');
      const screen = document.getElementById('loadingScreen');
ECHO is off.
      let width = 0;
      const interval = setInterval(() => {
        width += Math.random() * 10;
        if(width > 100) width = 100;
        bar.style.width = width + '%';
ECHO is off.
        if(width === 100) {
          clearInterval(interval);
          setTimeout(() => {
            screen.classList.add('hidden');
          }, 500);
        }
      }, 100);
ECHO is off.
      document.getElementById('year').textContent = new Date().getFullYear();
    });
    let clans = [];
    function val(id, type = 'float') {
      const el = document.getElementById(id);
      if (el) return 0;
      if (type === 'string') return el.value;
      if (type === 'bool') return el.value === 'true';
      if (type === 'list') return el.value ? el.value.split(',').map(s=>s.trim()).filter(s=>s) : [];
      return parseFloat(el.value) || 0;
    }
    function addClan() {
      const id = val('id', 'string').toLowerCase().replace(/\s/g, '_');
      if (id) {
        alert("Please provide a Clan ID.");
        return;
      }
      if(clans.some(c => c.id === id)) {
        if(confirm(`Clan ID '${id}' exists. Overwrite?`)) return;
        clans = clans.filter(c => c.id == id);
      }
      const clanData = {
        id: id,
        display_name: val('display_name', 'string'),
        color: val('color', 'string'),
        has_god_ki: val('has_god_ki', 'bool'),
        specializations: val('specializations', 'list'),
        default_traits: val('default_traits', 'list'),
        default_physical_traits: val('default_physical_traits', 'list'),
        damage_multiplier: val('damage_multiplier'),
        technique_cost_mult: val('technique_cost_mult'),
        domain_expansion_mult: val('domain_expansion_mult'),
        simple_domain_radius_mult: val('simple_domain_radius_mult'),
        rct_efficiency_mult: val('rct_efficiency_mult'),
        rct_fatigue_mult: val('rct_fatigue_mult'),
        da_drain_mult: val('da_drain_mult'),
        black_flash_chance_bonus: val('black_flash_chance_bonus'),
ECHO is off.
        simple_domain_level_bonus: 0.0,
        da_level_bonus: 0.0,
        guard_damage_mult: 1.0,
        guard_duration_mult: 1.0,
        weapon_damage_multiplier: val('damage_multiplier'), 
        bonus_health: val('bonus_health'),
        bonus_armor: val('bonus_armor'),
        bonus_attack_damage: val('bonus_attack_damage'),
        bonus_speed: val('bonus_speed'),
        bonus_knockback_resistance: 0.0
      };
      clans.push(clanData);
      renderList();
ECHO is off.
      document.getElementById('id').value = "";
      document.getElementById('display_name').value = "";
    }
    function renderList() {
      const container = document.getElementById('clanList');
      if(clans.length === 0) {
        container.innerHTML = '<span style="color:rgba(255,255,255,0.3); font-style: italic;">No clans added yet. Add one above</span>';
        return;
      }
      container.innerHTML = clans.map(c => `
        <div class="clan-card">
          <div class="clan-dot" style="background-color: ${c.color}; color: ${c.color}"></div>
          <div>
            <div style="font-weight:600; font-size:1.1rem;">${c.display_name}</div>
            <div style="font-size:0.8rem; opacity:0.7;">ID: ${c.id}</div>
          </div>
        </div>
      `).join('');
    }
    function clearList() {
      if(confirm("Remove all clans?")) {
        clans = [];
        renderList();
      }
    }
    function downloadPack() {
      if(clans.length === 0) {
        alert("Add at least one clan first.");
        return;
      }
      const zip = new JSZip();
      const packName = document.getElementById('pack_desc').value;
ECHO is off.
      zip.file("pack.mcmeta", JSON.stringify({
        pack: {
          pack_format: 15,
          description: packName
        }
      }, null, 2));
      const folder = zip.folder("data").folder("omega").folder("omega_clans");
ECHO is off.
      clans.forEach(c => {
        folder.file(`${c.id}.json`, JSON.stringify(c, null, 2));
      });
      zip.generateAsync({type:"blob"}).then(content => {
        saveAs(content, "omega_clans_datapack.zip");
      });
    }
  </script>
</body>
</html>
 
=== developerhtml === 
<DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Omega Developer API</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.7.0/styles/atom-one-dark.min.css">
  <style>
    :root {
      --primary-color: #a29bfe;
      --bg-color: #0a0a0a;
      --text-color: #ffffff;
      --card-bg: rgba(0, 0, 0, 0.7);
      --border-color: rgba(255, 255, 255, 0.1);
      --code-bg: #1e1e1e;
    }
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: 'Poppins', sans-serif; color: var(--text-color); background: var(--bg-color); line-height: 1.6; }
    .bg-video {
      position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -2;
      background: linear-gradient(135deg, #050505 0%, #1a1a1a 100%);
    }
    .bg-video video {
      min-width: 100%; min-height: 100%; width: auto; height: auto;
      position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);
      object-fit: cover; opacity: 0.3;
    }
    .container { max-width: 1100px; margin: 0 auto; padding: 2rem; position: relative; z-index: 1; }
    .dev-header { margin-bottom: 3rem; border-bottom: 1px solid var(--border-color); padding-bottom: 2rem; }
    .breadcrumb { color: #aaa; margin-bottom: 0.5rem; }
    .breadcrumb a { color: var(--primary-color); text-decoration: none; }
    h1 { font-size: 2.5rem; margin-bottom: 0.5rem; }
    .section { margin-bottom: 4rem; background: var(--card-bg); padding: 2rem; border-radius: 12px; border: 1px solid var(--border-color); }
    h2 { color: var(--primary-color); margin-bottom: 1.5rem; font-size: 1.8rem; }
    h3 { color: #dfe6e9; margin: 1.5rem 0 1rem; border-left: 3px solid var(--primary-color); padding-left: 10px; }
    .code-block { background: var(--code-bg); border-radius: 8px; border: 1px solid #333; margin: 1rem 0; overflow: hidden; }
    .code-header { background: #252526; padding: 0.5rem 1rem; font-size: 0.8rem; color: #888; display: flex; justify-content: space-between; align-items: center; }
    .copy-btn { background: none; border: none; color: #aaa; cursor: pointer; }
    .copy-btn:hover { color: white; }
    pre { margin: 0; padding: 1rem; overflow-x: auto; }
    .api-table { width: 100%; border-collapse: collapse; margin: 1.5rem 0; font-size: 0.9rem; }
    .api-table th, .api-table td { text-align: left; padding: 1rem; border-bottom: 1px solid rgba(255,255,255,0.1); }
    .api-table th { color: var(--primary-color); background: rgba(0,0,0,0.3); }
    .api-table code { background: rgba(255,255,255,0.1); padding: 2px 5px; border-radius: 4px; color: #fab1a0; font-family: 'Consolas', monospace; }
  </style>
</head>
<body>
  <div class="bg-video">
    <video autoplay muted loop><source src="images/background.mp4" type="video/mp4"></video>
  </div>
  <div class="container">
    <div class="dev-header">
      <div class="breadcrumb"><a href="index.html"><i class="fas fa-arrow-left"></i> Back to Hub</a> / Developer API</div>
      <h1>Java API Integration</h1>
      <p>Guide for mod developers to integrate with Slyrien's Omega via the Forge API.</p>
    </div>
    <div class="section">
      <h2>Getting Started</h2>
      <p>To create complex logic that cannot be achieved via JSON (e.g., custom on-hit effects, render logic), you must implement the provided Java interfaces and register them in your mod's main class.</p>
    </div>
    <div class="section">
      <h2>1. Creating a Custom Clan</h2>
      <p>Implement the <code>IClan</code> interface. The example below demonstrates every available multiplier method you can override to control Jujutsu mechanics.</p>
      <div class="code-block">
        <div class="code-header">MyCustomClan.java <button class="copy-btn" onclick="copyCode(this)"><i class="fas fa-copy"></i></button></div>
        <pre><code class="language-java">package com.example.addon.clans;
import net.slyrien.api.IClan;
import net.slyrien.capability.IOmegaPlayerData;
import net.minecraft.world.entity.LivingEntity;
import net.minecraft.network.chat.Component;
public class MyCustomClan implements IClan {
    @Override
    public String getClanId() {
        return "my_custom_clan";
    }
    @Override
    public Component getDisplayName() {
        return Component.literal("My Custom Clan");
    }
    // --- DAMAGE & COMBAT ---
    @Override // 1.0 = 100% (Normal). 1.5 = 150% Damage
    public float getDamageMultiplier(LivingEntity entity, IOmegaPlayerData cap, boolean isWeapon) {
        return isWeapon ? 1.0f : 1.2f; // 20% bonus to unarmed/technique damage only
    }
    @Override // Additive bonus (0.05 = +5% chance)
    public double getBlackFlashChanceBonus(LivingEntity entity, IOmegaPlayerData cap) {
        return 0.1; 
    }
    // --- DOMAIN EXPANSION & SIMPLE DOMAIN ---
    @Override // Multiplies the power/refinement of Domain Expansions
    public float getDomainExpansionMult(LivingEntity entity, IOmegaPlayerData cap) {
        return 1.1f;
    }
    @Override // Radius multiplier for Simple Domain
    public float getSimpleDomainRadiusMult(LivingEntity entity, IOmegaPlayerData cap) {
        return 1.5f;
    }
    @Override // Flat addition to Simple Domain output level
    public float getSimpleDomainLevelBonus(LivingEntity entity, IOmegaPlayerData cap) {
        return 1.0f;
    }
    // --- DOMAIN AMPLIFICATION ---
    @Override // Multiplier for CE drain rate (0.8 = 20% less drain)
    public float getDaDrainMult(LivingEntity entity, IOmegaPlayerData cap) {
        return 0.8f;
    }
    @Override // Flat addition to Domain Amplification output
    public float getDaLevelBonus(LivingEntity entity, IOmegaPlayerData cap) {
        return 0.0f;
    }
    // --- REVERSE CURSED TECHNIQUE (RCT) ---
    @Override // Multiplier for healing amount per tick
    public float getRctEfficiencyMult(LivingEntity entity, IOmegaPlayerData cap) {
        return 1.0f;
    }
    @Override // Multiplier for fatigue accumulation (lower is better)
    public float getRctFatigueMult(LivingEntity entity, IOmegaPlayerData cap) {
        return 0.9f; 
    }
    // --- DEFENSE & UTILITY ---
    @Override // Damage taken multiplier while guarding (0.5 = 50% damage reduction)
    public float getGuardDamageMult(LivingEntity entity, IOmegaPlayerData cap) {
        return 0.8f;
    }
    @Override // How long a guard lasts before breaking
    public float getGuardDurationMult(LivingEntity entity, IOmegaPlayerData cap) {
        return 1.2f;
    }
    @Override // Multiplier for Cursed Technique CE costs (0.5 = half cost)
    public float getTechniqueCostMult(LivingEntity entity, IOmegaPlayerData cap) {
        return 0.9f;
    }
    // --- EVENT HOOKS ---
    @Override
    public void onEntityAttack(LivingHurtEvent event, LivingEntity attacker, IOmegaPlayerData cap) {
        // Logic executed when clan member hits something
    }
}</code></pre>
      </div>
    </div>
    <div class="section">
      <h2>2. Custom Traits & Physical Traits</h2>
      <p>Traits function similarly to Clans but can be stacked on top of them. Use <code>ITrait</code> for spiritual abilities (e.g., "Angel") and <code>IPhysicalTrait</code> for bodily mutations (e.g., "Vessel"). They support all the multipliers listed in the Clan section.</p>
      <h3>Implementing a Trait</h3>
      <div class="code-block">
        <div class="code-header">MyCustomTrait.java <button class="copy-btn" onclick="copyCode(this)"><i class="fas fa-copy"></i></button></div>
        <pre><code class="language-java">import net.slyrien.api.ITrait;
public class MyCustomTrait implements ITrait {
    @Override
    public String getTraitId() {
        return "divine_protection";
    }
    @Override
    public float getGuardDamageMult(LivingEntity entity, IOmegaPlayerData cap) {
        // Traits stack multiplicatively
        return 0.5f; // Takes 50% damage while guarding
    }
    @Override
    public void onAdd(LivingEntity entity, IOmegaPlayerData capability) {
        // Give permanent potion effects or attributes here
    }
}</code></pre>
      </div>
      <h3>Implementing a Physical Trait</h3>
      <div class="code-block">
        <div class="code-header">MyPhysicalTrait.java <button class="copy-btn" onclick="copyCode(this)"><i class="fas fa-copy"></i></button></div>
        <pre><code class="language-java">import net.slyrien.api.IPhysicalTrait;
public class MyPhysicalTrait implements IPhysicalTrait {
    @Override
    public String getPhysicalTraitId() {
        return "steel_skin";
    }
    @Override
    public float getDamageMultiplier(LivingEntity entity, IOmegaPlayerData cap, boolean isWeapon) {
        return isWeapon ? 1.0f : 1.5f; // 50% bonus to unarmed damage
    }
    @Override
    public void onEntityTick(LivingEntity entity, IOmegaPlayerData capability) {
        if(entity.tickCount % 20 == 0) {
            // Regeneration logic
        }
    }
}</code></pre>
      </div>
    </div>
    <div class="section">
      <h2>3. Creating Custom Souls</h2>
      <p>Souls (<code>ISoul</code>) represent absorbed entities like Sukuna fingers. They have unique methods for audio and dialogue triggers in addition to the standard stat multipliers.</p>
      <div class="code-block">
        <div class="code-header">AncientSorcererSoul.java <button class="copy-btn" onclick="copyCode(this)"><i class="fas fa-copy"></i></button></div>
        <pre><code class="language-java">import net.slyrien.api.ISoul;
import net.slyrien.util.souls.DialogueTrigger;
public class AncientSorcererSoul implements ISoul {
    @Override
    public String getSoulId() { return "ancient_sorcerer"; }
    @Override
    public Component getDisplayName() { return Component.literal("The Ancient One"); }
    @Override
    public int getColor() { return 0xFF0000; } // Chat color for dialogue
    @Override
    public SoundEvent getBlipSound() {
        return SoundEvents.EXPERIENCE_ORB_PICKUP; // Sound played when dialogue types out
    }
    // --- Dialogue Logic ---
    @Override
    public List<String> getDialogue(DialogueTrigger trigger) {
        switch (trigger) {
            case COMBAT_START:
                return List.of("Show me your strength", "Do not disappoint me.");
            case PLAYER_LOW_HEALTH:
                return List.of("Stand up. We are not finished.");
            case TARGET_LOW_HEALTH:
                return List.of("Finish them.");
            case PLAYER_KILL_BOSS:
                return List.of("A worthy sacrifice.");
            default:
                return Collections.emptyList();
        }
    }
    // --- Stats ---
    @Override
    public float getHealthBonus() { return 20.0f; } // +10 Hearts
    @Override
    public float getDomainExpansionMult(LivingEntity entity, IOmegaPlayerData cap) {
        return 1.2f; // 20% stronger domains
    }
}</code></pre>
      </div>
    </div>
    <div class="section">
      <h2>4. Global Modifiers (Advanced & Unorthodox)</h2>
      <p><code>IOmegaGlobalModifier</code> applies rules to <strong>every</strong> entity in the game. While simple uses include global nerfs, this interface allows you to implement complex logic based on NBT data, dimensions, or specific technique interactions, effectively letting you create custom game modes or overhaul existing mechanics.</p>
      <div class="code-block">
        <div class="code-header">UnorthodoxGlobalLogic.java <button class="copy-btn" onclick="copyCode(this)"><i class="fas fa-copy"></i></button></div>
        <pre><code class="language-java">import net.slyrien.api.IOmegaGlobalModifier;
import net.minecraft.world.entity.LivingEntity;
public class UnorthodoxGlobalLogic implements IOmegaGlobalModifier {
    @Override
    public float getDamageMultiplier(LivingEntity entity, IOmegaPlayerData cap, boolean isWeapon) {
        // Feature: "Culling Game" Event Logic
        // Checks a global GameRule or a tag on the player indicating they are a participant
        if (entity.getPersistentData().getBoolean("InCullingGame")) {
            // Unorthodox: Damage increases as health gets lower (Berserker Mechanic)
            float missingHp = (entity.getMaxHealth() - entity.getHealth()) / entity.getMaxHealth();
            return 1.0f + missingHp; // Up to 2x damage at 0 HP
        }
        return 1.0f;
    }
    @Override
    public float getTechniqueCostMult(LivingEntity entity, IOmegaPlayerData cap) {
        // Technique Interaction: "Binding Vow"
        // If the player has a specific custom NBT tag (set by a command or item)
        if (entity.getPersistentData().getBoolean("BindingVow_Overclock")) {
            return 2.0f; // Double CE Cost
        }
        // Environment Interaction: Nether buffs Fire techniques (Jogo / Disaster Flames)
        double techniqueId = entity.getPersistentData().getDouble("PlayerCurseTechnique");
        if (techniqueId == 4.0 && entity.level().dimension() == Level.NETHER) { // ID 4 = Disaster Flames
            return 0.5f; // Half cost for Jogo in the Nether
        }
        return 1.0f;
    }
    @Override
    public float getRctEfficiencyMult(LivingEntity entity, IOmegaPlayerData cap) {
        // Feature: "Cursed Realm" Dimension
        // Disable RCT entirely in a specific dimension
        if (entity.level().dimension().location().toString().equals("my_mod:cursed_realm")) {
            return 0.0f; 
        }
        // Unorthodox: Binding Vow Reward (from above)
        if (entity.getPersistentData().getBoolean("BindingVow_Overclock")) {
            return 1.5f; // 50% better healing in exchange for double cost
        }
        return 1.0f;
    }
}</code></pre>
      </div>
    </div>
    <div class="section">
      <h2>5. Registering Content</h2>
      <p>Finally, register all your custom implementations in your mod's constructor to load them into Omega.</p>
      <div class="code-block">
        <div class="code-header">ExampleMod.java <button class="copy-btn" onclick="copyCode(this)"><i class="fas fa-copy"></i></button></div>
        <pre><code class="language-java">@Mod("examplemod")
public class ExampleMod {
    public ExampleMod() {
        // Register Clans
        SlyrienOmegaAPI.registerClan(new MyCustomClan());
        // Register Traits
        SlyrienOmegaAPI.registerTrait(new MyCustomTrait());
        SlyrienOmegaAPI.registerPhysicalTrait(new MyPhysicalTrait());
        // Register Souls
        SlyrienOmegaAPI.registerSoul(new AncientSorcererSoul());
        // Register Global Logic
        SlyrienOmegaAPI.registerGlobalModifier(new UnorthodoxGlobalLogic());
    }
}</code></pre>
      </div>
    </div>
  </div>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.7.0/highlight.min.js"></script>
  <script>
    hljs.highlightAll();
    function copyCode(btn) {
      const pre = btn.parentElement.nextElementSibling;
      navigator.clipboard.writeText(pre.innerText).then(() => {
        btn.innerHTML = '<i class="fas fa-check"></i>';
        setTimeout(() => btn.innerHTML = '<i class="fas fa-copy"></i>', 2000);
      });
    }
  </script>
</body>
</html>
 
=== indexhtml === 
<DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Omega API & Tools Hub</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    :root {
      --primary-color: #6c5ce7;
      --accent-color: #00cec9;
      --bg-color: #0a0a0a;
      --text-color: #ffffff;
      --card-bg: rgba(0, 0, 0, 0.7);
      --border-color: rgba(255, 255, 255, 0.1);
    }
    * { box-sizing: border-box; margin: 0; padding: 0; }
    ::-webkit-scrollbar { width: 10px; }
    ::-webkit-scrollbar-track { background: #111; }
    ::-webkit-scrollbar-thumb { background: var(--primary-color); border-radius: 5px; }
    body {
      font-family: 'Poppins', sans-serif;
      color: var(--text-color);
      background: var(--bg-color);
      overflow-x: hidden;
      line-height: 1.6;
    }
    /* Background Video */
    .bg-video {
      position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -2;
      background: linear-gradient(135deg, #050505 0%, #1a1a1a 100%);
    }
    .bg-video video {
      min-width: 100%; min-height: 100%; width: auto; height: auto;
      position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);
      object-fit: cover; opacity: 0.3;
    }
    .container { max-width: 1200px; margin: 0 auto; padding: 2rem; position: relative; z-index: 1; }
ECHO is off.
    /* Header */
    header { text-align: center; padding: 4rem 0; animation: slideDown 1s ease-out; }
    @keyframes slideDown { from { transform: translateY(-50px); opacity: 0; } to { transform: translateY(0); opacity: 1; } }
    .logo { font-size: 3.5rem; font-weight: 700; text-shadow: 0 0 20px rgba(108, 92, 231, 0.6); }
    .logo span { color: var(--primary-color); }
    .tagline { color: #aaa; font-size: 1.2rem; max-width: 600px; margin: 0 auto; }
    /* Section Styles */
    .section-title { 
      font-size: 1.8rem; margin: 3rem 0 2rem; 
      border-left: 5px solid var(--accent-color); 
      padding-left: 1rem; color: white; 
    }
    /* Grid Layout */
    .tools-grid {
      display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2rem; margin-bottom: 2rem;
    }
    .tool-card {
      background: var(--card-bg); border-radius: 16px; padding: 2rem;
      border: 1px solid var(--border-color); backdrop-filter: blur(10px);
      text-decoration: none; color: white; transition: 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative; overflow: hidden; display: flex; flex-direction: column;
    }
    .tool-card:hover { transform: translateY(-10px); border-color: var(--primary-color); box-shadow: 0 10px 30px rgba(108, 92, 231, 0.2); }
    .tool-icon { font-size: 2.5rem; margin-bottom: 1rem; color: var(--primary-color); }
    .tool-name { font-size: 1.4rem; font-weight: 600; margin-bottom: 0.5rem; }
    .tool-desc { font-size: 0.9rem; color: #bbb; margin-bottom: 1.5rem; flex-grow: 1; }
    .tool-link { font-weight: 600; color: var(--accent-color); display: flex; align-items: center; gap: 0.5rem; margin-top: auto;}
    /* Resource Cards */
    .resource-grid {
        display: grid; grid-template-columns: 1fr 1fr; gap: 2rem;
    }
    @media (max-width: 768px) { .resource-grid { grid-template-columns: 1fr; } }
    .resource-card {
        background: linear-gradient(145deg, rgba(108, 92, 231, 0.1), rgba(0, 0, 0, 0.6));
        border: 1px solid var(--border-color);
        border-radius: 16px; padding: 2.5rem;
        text-decoration: none; color: white;
        transition: 0.3s ease; display: flex; align-items: center; gap: 2rem;
    }
    .resource-card:hover {
        background: linear-gradient(145deg, rgba(108, 92, 231, 0.2), rgba(0, 0, 0, 0.8));
        border-color: var(--accent-color);
    }
    .resource-icon { font-size: 3rem; color: var(--accent-color); }
    .resource-content h3 { font-size: 1.5rem; margin-bottom: 0.5rem; }
    .resource-content p { color: #aaa; font-size: 0.95rem; }
    footer { text-align: center; margin-top: 5rem; color: #555; padding-bottom: 2rem; }
  </style>
</head>
<body>
  <div class="bg-video">
    <video autoplay muted loop><source src="images/background.mp4" type="video/mp4"></video>
  </div>
  <div class="container">
    <header>
      <div class="logo">Slyrien's <span>Omega</span> API</div>
      <p class="tagline">The central hub for creating Clans, Souls, and extending the Omega Mod.</p>
    </header>
    <h2 class="section-title">Datapack Generators</h2>
    <div class="tools-grid">
      <a href="clans.html" class="tool-card">
        <i class="fas fa-users tool-icon"></i>
        <div class="tool-name">Clan Generator</div>
        <div class="tool-desc">Create custom clans with specific stat modifiers, Jujutsu multipliers, and starting traits.</div>
        <div class="tool-link">Launch Tool <i class="fas fa-arrow-right"></i></div>
      </a>
      <a href="specs.html" class="tool-card">
        <i class="fas fa-bolt tool-icon" style="color: #ff9f43;"></i>
        <div class="tool-name">Specializations</div>
        <div class="tool-desc">Define sub-paths for clans (e.g. Sage, Destroyer) to allow further player progression.</div>
        <div class="tool-link" style="color: #ff9f43;">Launch Tool <i class="fas fa-arrow-right"></i></div>
      </a>
      <a href="souls.html" class="tool-card">
        <i class="fas fa-ghost tool-icon" style="color: #e056fd;"></i>
        <div class="tool-name">Soul Generator</div>
        <div class="tool-desc">Design unique souls with audio cues, custom dialogue triggers, and passive buffs.</div>
        <div class="tool-link" style="color: #e056fd;">Launch Tool <i class="fas fa-arrow-right"></i></div>
      </a>
      <a href="afo.html" class="tool-card">
        <i class="fas fa-hand-holding-magic tool-icon" style="color: #ff4757;"></i>
        <div class="tool-name">AFO Mappings</div>
        <div class="tool-desc">Configure techniques, skills, and mob drops for the All For One ability system.</div>
        <div class="tool-link" style="color: #ff4757;">Launch Tool <i class="fas fa-arrow-right"></i></div>
      </a>
    </div>
    <h2 class="section-title">Documentation & Resources</h2>
    <div class="resource-grid">
        <a href="wiki.html" class="resource-card">
            <i class="fas fa-book resource-icon"></i>
            <div class="resource-content">
                <h3>Datapack Wiki</h3>
                <p>JSON Specifications, file structures, and reference lists for creating custom content without code.</p>
            </div>
        </a>
        <a href="developer.html" class="resource-card">
            <i class="fas fa-code resource-icon"></i>
            <div class="resource-content">
                <h3>Developer API</h3>
                <p>Java documentation for addon developers. Implement custom interfaces, handle events, and access NBT data.</p>
            </div>
        </a>
    </div>
    <footer>
      &copy; <span id="year"></span> Slyrien's Omega Utilities. All rights reserved
    </footer>
  </div>
  <script>
    document.getElementById('year').textContent = new Date().getFullYear();
  </script>
</body>
</html>
 
=== soulshtml === 
<DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Omega Soul Generator</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    :root {
      --primary-color: #e056fd; /* Different shade for Souls */
      --secondary-color: #ffffff;
      --background-color: #0a0a0a;
      --text-color: #ffffff;
      --hover-color: #be2edd;
      --card-bg: rgba(0, 0, 0, 0.6);
      --input-bg: rgba(255, 255, 255, 0.05);
      --text-light: #dfe6e9;
    }
    * { box-sizing: border-box; margin: 0; padding: 0; scrollbar-width: thin; scrollbar-color: var(--primary-color) rgba(0,0,0,0.3); }
    ::-webkit-scrollbar { width: 12px; }
    ::-webkit-scrollbar-track { background: rgba(0,0,0,0.3); }
    ::-webkit-scrollbar-thumb { background: var(--primary-color); border-radius: 6px; border: 2px solid rgba(0,0,0,0.3); }
    body { font-family: 'Poppins', sans-serif; color: var(--text-color); background: var(--background-color); overflow-x: hidden; line-height: 1.6; }
    /* Loading */
    .loading-screen { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: #000; display: flex; flex-direction: column; justify-content: center; align-items: center; z-index: 9999; transition: opacity 0.8s; }
    .loading-screen.hidden { opacity: 0; visibility: hidden; }
    .loading-bar-container { width: 300px; height: 4px; background: #333; margin-top: 20px; border-radius: 2px; }
    .loading-bar { width: 0%; height: 100%; background: var(--primary-color); transition: width 0.5s; }
    /* Layout */
    .bg-video { position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -1; opacity: 0.3; object-fit: cover; }
    .container { max-width: 1100px; margin: 0 auto; padding: 2rem; min-height: 100vh; display: flex; flex-direction: column; }
ECHO is off.
    .main-content { background: var(--card-bg); border-radius: 16px; padding: 3rem; backdrop-filter: blur(12px); border: 1px solid rgba(255,255,255,0.1); animation: fadeIn 1s ease-out; }
    @keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
    h1 { font-size: 2.5rem; text-shadow: 0 2px 10px rgba(224, 86, 253, 0.5); }
    h2 { font-size: 1.5rem; color: var(--primary-color); margin: 2rem 0 1rem; border-bottom: 1px solid rgba(255,255,255,0.1); display: inline-block; padding-bottom: 5px; }
    /* Forms */
    .form-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1.5rem; }
    .input-group { display: flex; flex-direction: column; gap: 0.5rem; }
    label { font-size: 0.85rem; color: var(--text-light); }
    input, select, textarea { background: var(--input-bg); border: 1px solid rgba(255,255,255,0.1); color: white; padding: 0.8rem; border-radius: 8px; font-family: inherit; width: 100%; }
    input:focus, select:focus, textarea:focus { outline: none; border-color: var(--primary-color); box-shadow: 0 0 15px rgba(224, 86, 253, 0.2); }
ECHO is off.
    /* Dialogue Section */
    .dialogue-box { background: rgba(0,0,0,0.3); padding: 1.5rem; border-radius: 12px; border: 1px dashed rgba(255,255,255,0.2); margin-top: 1rem; }
    .tag { background: var(--primary-color); padding: 2px 8px; border-radius: 4px; font-size: 0.8rem; margin-right: 5px; color: black; font-weight: bold; }
    /* Controls */
    .controls { margin-top: 2rem; display: flex; gap: 1rem; flex-wrap: wrap; }
    .btn { background: var(--primary-color); color: white; padding: 1rem 2rem; border-radius: 50px; text-decoration: none; font-weight: 600; cursor: pointer; border: none; transition: 0.3s; }
    .btn:hover { transform: translateY(-3px); box-shadow: 0 5px 15px rgba(224, 86, 253, 0.4); }
    .btn-download { background: #00cec9; }
    /* List */
    .item-list { display: flex; flex-wrap: wrap; gap: 1rem; margin-top: 2rem; }
    .item-card { background: rgba(255,255,255,0.05); padding: 1rem; border-radius: 12px; border: 1px solid rgba(255,255,255,0.1); display: flex; align-items: center; gap: 1rem; }
    .item-color { width: 15px; height: 15px; border-radius: 50%; box-shadow: 0 0 10px currentColor; }
  </style>
</head>
<body>
  <div id="loadingScreen" class="loading-screen">
    <h1 style="color:white;">Omega Souls</h1>
    <div class="loading-bar-container"><div id="loadingBar" class="loading-bar"></div></div>
  </div>
  <video class="bg-video" autoplay muted loop><source src="images/background.mp4" type="video/mp4"></video>
  <div class="container">
    <main class="main-content">
      <h1>Soul Shard Generator</h1>
      <p style="color:#aaa; margin-bottom:2rem;">Create custom souls with unique stats, audio cues, and dialogue interactions.</p>
      <form id="soulForm">
        <-- Identity -->
        <h2>Identity</h2>
        <div class="form-grid">
          <div class="input-group"><label>Soul ID</label><input type="text" id="id" placeholder="e.g. sukuna_finger" required></div>
          <div class="input-group"><label>Display Name</label><input type="text" id="name" placeholder="e.g. King of Curses"></div>
          <div class="input-group"><label>Soul Color</label><input type="color" id="color" value="#e056fd" style="height:45px; padding:0 5px;"></div>
          <div class="input-group"><label>Has God Ki?</label>
            <select id="has_god_ki"><option value="false">No</option><option value="true">Yes</option></select>
          </div>
        </div>
        <-- Audio -->
        <h2>Audio FX</h2>
        <div class="form-grid">
          <div class="input-group"><label>Primary Sound</label><input type="text" id="blip_sound" value="omega:maleblip"></div>
          <div class="input-group"><label>Pitch</label><input type="number" id="blip_pitch" value="1.0" step="0.1"></div>
          <div class="input-group"><label>Volume</label><input type="number" id="blip_volume" value="1.0" step="0.1"></div>
          <div class="input-group"><label>Secondary Sound (Optional)</label><input type="text" id="sec_sound" placeholder="omega:femaleblip"></div>
        </div>
        <-- Stats -->
        <h2>Multipliers (1.0 = Normal)</h2>
        <div class="form-grid">
          <div class="input-group"><label>Dmg Mult</label><input type="number" id="damage_multiplier" value="1.0" step="0.1"></div>
          <div class="input-group"><label>Weapon Dmg Mult</label><input type="number" id="weapon_damage_multiplier" value="1.0" step="0.1"></div>
          <div class="input-group"><label>Simple Domain Radius</label><input type="number" id="simple_domain_radius_mult" value="1.0" step="0.1"></div>
          <div class="input-group"><label>Domain Expansion</label><input type="number" id="domain_expansion_mult" value="1.0" step="0.1"></div>
          <div class="input-group"><label>RCT Efficiency</label><input type="number" id="rct_efficiency_mult" value="1.0" step="0.1"></div>
          <div class="input-group"><label>DA Drain</label><input type="number" id="da_drain_mult" value="1.0" step="0.1"></div>
        </div>
        <-- Attributes -->
        <h2>Passive Bonuses</h2>
        <div class="form-grid">
          <div class="input-group"><label>Bonus Health</label><input type="number" id="health_bonus" value="0"></div>
          <div class="input-group"><label>Bonus Armor</label><input type="number" id="armor_bonus" value="0"></div>
          <div class="input-group"><label>Bonus Attack</label><input type="number" id="attack_bonus" value="0"></div>
          <div class="input-group"><label>Bonus Speed</label><input type="number" id="speed_bonus" value="0" step="0.01"></div>
        </div>
        <-- Dialogue -->
        <h2>Soul Dialogue</h2>
        <div class="dialogue-box">
          <div class="form-grid">
            <div class="input-group">
              <label>Trigger Event</label>
              <select id="dialogue_trigger">
                <option value="COMBAT_START">Combat Start</option>
                <option value="PLAYER_LOW_HEALTH">Player Low HP</option>
                <option value="TARGET_LOW_HEALTH">Enemy Low HP</option>
                <option value="PLAYER_KILL_MOB">Kill Mob</option>
                <option value="PLAYER_KILL_BOSS">Kill Boss</option>
                <option value="PLAYER_DEATH">Player Death</option>
                <option value="IDLE">Idle (Rare)</option>
                <option value="NIGHT_TIME">Night Time</option>
              </select>
            </div>
            <div class="input-group">
              <label>Lines (One per line)</label>
              <textarea id="dialogue_lines" rows="3" placeholder="I am the strongest...&#10;Pathetic creature."></textarea>
            </div>
          </div>
          <div style="margin-top:10px; text-align:right;">
            <button type="button" class="btn" style="padding:0.5rem 1rem; font-size:0.8rem;" onclick="saveDialogue()">Save Trigger</button>
          </div>
          <div id="savedDialogues" style="margin-top:10px; font-size:0.9rem; color:#aaa;"></div>
        </div>
        <div class="controls">
          <button type="button" class="btn" onclick="addSoul()">
            <i class="fas fa-ghost"></i> Add Soul
          </button>
        </div>
      </form>
      <div id="itemList" class="item-list">
        <span style="color:rgba(255,255,255,0.3);">No souls added yet.</span>
      </div>
      <div class="controls" style="border-top: 1px solid rgba(255,255,255,0.1); padding-top: 2rem;">
        <div class="input-group" style="max-width:300px;">
          <label>Pack Name</label>
          <input type="text" id="pack_desc" value="Omega Souls">
        </div>
        <button type="button" class="btn btn-download" onclick="downloadPack()">Download Datapack</button>
      </div>
    </main>
  </div>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/FileSaver.js/2.0.5/FileSaver.min.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      let width = 0;
      const interval = setInterval(() => {
        width += 20; document.getElementById('loadingBar').style.width = width + '%';
        if(width >= 100) { clearInterval(interval); setTimeout(() => document.getElementById('loadingScreen').classList.add('hidden'), 500); }
      }, 150);
    });
    let souls = [];
    let currentDialogues = {};
    function val(id, type = 'float') {
      const el = document.getElementById(id);
      if(type === 'string') return el.value;
      if(type === 'bool') return el.value === 'true';
      return parseFloat(el.value) || 0;
    }
    function saveDialogue() {
      const trigger = document.getElementById('dialogue_trigger').value;
      const lines = document.getElementById('dialogue_lines').value.split('\n').filter(l => l.trim());
      if(lines.length > 0) {
        currentDialogues[trigger] = lines;
        const div = document.getElementById('savedDialogues');
        div.innerHTML += `<span class="tag">${trigger}</span> `;
        document.getElementById('dialogue_lines').value = "";
      }
    }
    function addSoul() {
      const id = val('id', 'string').toLowerCase().replace(/\s/g, '_');
      if(id) return alert("ID required");
      const soul = {
        id: id,
        name: val('name', 'string'),
        color: val('color', 'string'),
        has_god_ki: val('has_god_ki', 'bool'),
        blip_sound: val('blip_sound', 'string'),
        blip_pitch: val('blip_pitch'),
        blip_volume: val('blip_volume'),
        secondary_blip_sound: val('sec_sound', 'string') || null,
ECHO is off.
        damage_multiplier: val('damage_multiplier'),
        weapon_damage_multiplier: val('weapon_damage_multiplier'),
        simple_domain_radius_mult: val('simple_domain_radius_mult'),
        domain_expansion_mult: val('domain_expansion_mult'),
        rct_efficiency_mult: val('rct_efficiency_mult'),
        da_drain_mult: val('da_drain_mult'),
ECHO is off.
        health_bonus: val('health_bonus'),
        armor_bonus: val('armor_bonus'),
        attack_bonus: val('attack_bonus'),
        speed_bonus: val('speed_bonus'),
ECHO is off.
        dialogue: Object.keys(currentDialogues).length > 0 ? {...currentDialogues} : null
      };
      souls.push(soul);
ECHO is off.
      // Render
      const list = document.getElementById('itemList');
      if(souls.length === 1) list.innerHTML = '';
      list.innerHTML += `
        <div class="item-card">
          <div class="item-color" style="background-color:${soul.color}; color:${soul.color}"></div>
          <div><strong>${soul.name}</strong><br><span style="font-size:0.8em; opacity:0.7;">${soul.id}</span></div>
        </div>
      `;
      // Reset
      document.getElementById('id').value = "";
      document.getElementById('name').value = "";
      currentDialogues = {};
      document.getElementById('savedDialogues').innerHTML = "";
    }
    function downloadPack() {
      if(souls.length === 0) return alert("Add souls first");
      const zip = new JSZip();
      zip.file("pack.mcmeta", JSON.stringify({ pack: { pack_format: 15, description: document.getElementById('pack_desc').value } }, null, 2));
      const folder = zip.folder("data").folder("omega").folder("omega_souls");
      souls.forEach(s => folder.file(`${s.id}.json`, JSON.stringify(s, null, 2)));
      zip.generateAsync({type:"blob"}).then(c => saveAs(c, "omega_souls_datapack.zip"));
    }
  </script>
</body>
</html>
 
=== specshtml === 
<DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Clan Specialization Generator</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    :root {
      --primary-color: #ff9f43; 
      --secondary-color: #ffffff;
      --background-color: #0a0a0a;
      --text-color: #ffffff;
      --hover-color: #feca57;
      --card-bg: rgba(0, 0, 0, 0.6);
      --input-bg: rgba(255, 255, 255, 0.05);
      --text-light: #dfe6e9;
    }
    * { box-sizing: border-box; margin: 0; padding: 0; scrollbar-width: thin; scrollbar-color: var(--primary-color) rgba(0,0,0,0.3); }
    ::-webkit-scrollbar { width: 12px; }
    ::-webkit-scrollbar-track { background: rgba(0,0,0,0.3); }
    ::-webkit-scrollbar-thumb { background: var(--primary-color); border-radius: 6px; border: 2px solid rgba(0,0,0,0.3); }
    body { font-family: 'Poppins', sans-serif; color: var(--text-color); background: var(--background-color); overflow-x: hidden; line-height: 1.6; }
    .loading-screen { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: #000; display: flex; flex-direction: column; justify-content: center; align-items: center; z-index: 9999; transition: opacity 0.8s; }
    .loading-screen.hidden { opacity: 0; visibility: hidden; }
    .loading-bar-container { width: 300px; height: 4px; background: #333; margin-top: 20px; border-radius: 2px; }
    .loading-bar { width: 0%; height: 100%; background: var(--primary-color); transition: width 0.5s; }
    .bg-video { position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -1; opacity: 0.3; object-fit: cover; }
    .container { max-width: 1100px; margin: 0 auto; padding: 2rem; min-height: 100vh; display: flex; flex-direction: column; }
ECHO is off.
    .main-content { background: var(--card-bg); border-radius: 16px; padding: 3rem; backdrop-filter: blur(12px); border: 1px solid rgba(255,255,255,0.1); animation: fadeIn 1s ease-out; }
    @keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
    h1 { font-size: 2.5rem; text-shadow: 0 2px 10px rgba(255, 159, 67, 0.5); }
    h2 { font-size: 1.5rem; color: var(--primary-color); margin: 2rem 0 1rem; border-bottom: 1px solid rgba(255,255,255,0.1); display: inline-block; padding-bottom: 5px; }
    .form-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 1.5rem; }
    .input-group { display: flex; flex-direction: column; gap: 0.5rem; }
    label { font-size: 0.85rem; color: var(--text-light); }
    input { background: var(--input-bg); border: 1px solid rgba(255,255,255,0.1); color: white; padding: 0.8rem; border-radius: 8px; font-family: inherit; width: 100%; }
    input:focus { outline: none; border-color: var(--primary-color); box-shadow: 0 0 15px rgba(255, 159, 67, 0.2); }
    .controls { margin-top: 2rem; display: flex; gap: 1rem; flex-wrap: wrap; }
    .btn { background: var(--primary-color); color: #000; padding: 1rem 2rem; border-radius: 50px; text-decoration: none; font-weight: 700; cursor: pointer; border: none; transition: 0.3s; }
    .btn:hover { transform: translateY(-3px); box-shadow: 0 5px 15px rgba(255, 159, 67, 0.4); }
    .btn-download { background: #00cec9; color: white; }
    .item-list { display: flex; flex-wrap: wrap; gap: 1rem; margin-top: 2rem; }
    .item-card { background: rgba(255,255,255,0.05); padding: 1rem 1.5rem; border-radius: 12px; border: 1px solid rgba(255,255,255,0.1); display: flex; align-items: center; gap: 1rem; }
    .spec-icon { font-size: 1.5rem; color: var(--primary-color); }
  </style>
</head>
<body>
  <div id="loadingScreen" class="loading-screen">
    <h1 style="color:white;">Clan Specializations</h1>
    <div class="loading-bar-container"><div id="loadingBar" class="loading-bar"></div></div>
  </div>
  <video class="bg-video" autoplay muted loop><source src="images/background.mp4" type="video/mp4"></video>
  <div class="container">
    <main class="main-content">
      <h1>Specialization Generator</h1>
      <p style="color:#aaa; margin-bottom:2rem;">Define specialized paths for your clans (e.g. Sage, Destroyer) with unique stat blocks.</p>
      <form id="specForm">
        <h2>Identity</h2>
        <div class="form-grid">
          <div class="input-group"><label>Spec ID (e.g. zenin_speed)</label><input type="text" id="id" required></div>
          <div class="input-group"><label>Display Name</label><input type="text" id="display_name" placeholder="Path of Speed"></div>
        </div>
        <h2>Modifiers (1.0 = Normal)</h2>
        <div class="form-grid">
          <div class="input-group"><label>Damage Mult</label><input type="number" id="damage_multiplier" value="1.0" step="0.1"></div>
          <div class="input-group"><label>Tech Cost Mult</label><input type="number" id="technique_cost_mult" value="1.0" step="0.1"></div>
          <div class="input-group"><label>Domain Power</label><input type="number" id="domain_expansion_mult" value="1.0" step="0.1"></div>
          <div class="input-group"><label>RCT Efficiency</label><input type="number" id="rct_efficiency_mult" value="1.0" step="0.1"></div>
          <div class="input-group"><label>DA Drain</label><input type="number" id="da_drain_mult" value="1.0" step="0.1"></div>
          <div class="input-group"><label>Guard Duration</label><input type="number" id="guard_duration_mult" value="1.0" step="0.1"></div>
        </div>
        <h2>Attribute Bonuses</h2>
        <div class="form-grid">
          <div class="input-group"><label>Bonus Health</label><input type="number" id="bonus_health" value="0"></div>
          <div class="input-group"><label>Bonus Armor</label><input type="number" id="bonus_armor" value="0"></div>
          <div class="input-group"><label>Bonus Attack</label><input type="number" id="bonus_attack_damage" value="0"></div>
          <div class="input-group"><label>Bonus Speed</label><input type="number" id="bonus_speed" value="0" step="0.01"></div>
        </div>
        <div class="controls">
          <button type="button" class="btn" onclick="addSpec()">
            <i class="fas fa-bolt"></i> Add Specialization
          </button>
        </div>
      </form>
      <div id="itemList" class="item-list">
        <span style="color:rgba(255,255,255,0.3);">No specializations added yet.</span>
      </div>
      <div class="controls" style="border-top: 1px solid rgba(255,255,255,0.1); padding-top: 2rem;">
        <div class="input-group" style="max-width:300px;">
          <label>Pack Name</label>
          <input type="text" id="pack_desc" value="Omega Specializations">
        </div>
        <button type="button" class="btn btn-download" onclick="downloadPack()">Download Datapack</button>
      </div>
    </main>
  </div>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/FileSaver.js/2.0.5/FileSaver.min.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      let width = 0;
      const interval = setInterval(() => {
        width += 20; document.getElementById('loadingBar').style.width = width + '%';
        if(width >= 100) { clearInterval(interval); setTimeout(() => document.getElementById('loadingScreen').classList.add('hidden'), 500); }
      }, 150);
    });
    let specs = [];
    function val(id, type = 'float') {
      const el = document.getElementById(id);
      if(type === 'string') return el.value;
      return parseFloat(el.value) || 0;
    }
    function addSpec() {
      const id = val('id', 'string').toLowerCase().replace(/\s/g, '_');
      if(id) return alert("ID required");
      const spec = {
        id: id,
        display_name: val('display_name', 'string'),
        damage_multiplier: val('damage_multiplier'),
        technique_cost_mult: val('technique_cost_mult'),
        domain_expansion_mult: val('domain_expansion_mult'),
        rct_efficiency_mult: val('rct_efficiency_mult'),
        da_drain_mult: val('da_drain_mult'),
        guard_duration_mult: val('guard_duration_mult'),
        bonus_health: val('bonus_health'),
        bonus_armor: val('bonus_armor'),
        bonus_attack_damage: val('bonus_attack_damage'),
        bonus_speed: val('bonus_speed'),
        // Defaults for unused
        simple_domain_radius_mult: 1.0,
        simple_domain_level_bonus: 0.0,
        da_level_bonus: 0.0,
        rct_fatigue_mult: 1.0,
        guard_damage_mult: 1.0,
        weapon_damage_multiplier: val('damage_multiplier'),
        black_flash_chance_bonus: 0.0,
        bonus_knockback_resistance: 0.0
      };
      specs.push(spec);
ECHO is off.
      const list = document.getElementById('itemList');
      if(specs.length === 1) list.innerHTML = '';
      list.innerHTML += `
        <div class="item-card">
          <i class="fas fa-bolt spec-icon"></i>
          <div><strong>${spec.display_name}</strong><br><span style="font-size:0.8em; opacity:0.7;">${spec.id}</span></div>
        </div>
      `;
ECHO is off.
      document.getElementById('id').value = "";
      document.getElementById('display_name').value = "";
    }
    function downloadPack() {
      if(specs.length === 0) return alert("Add specs first");
      const zip = new JSZip();
      zip.file("pack.mcmeta", JSON.stringify({ pack: { pack_format: 15, description: document.getElementById('pack_desc').value } }, null, 2));
      const folder = zip.folder("data").folder("omega").folder("omega_clan_specializations");
      specs.forEach(s => folder.file(`${s.id}.json`, JSON.stringify(s, null, 2)));
      zip.generateAsync({type:"blob"}).then(c => saveAs(c, "omega_specs_datapack.zip"));
    }
  </script>
</body>
</html>
 
=== wikihtml === 
<DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Omega Wiki - Datapacks</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.7.0/styles/atom-one-dark.min.css">
  <style>
    :root {
      --primary-color: #00cec9;
      --bg-color: #0a0a0a;
      --text-color: #ffffff;
      --card-bg: rgba(0, 0, 0, 0.7);
      --border-color: rgba(255, 255, 255, 0.1);
      --code-bg: #1e1e1e;
    }
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: 'Poppins', sans-serif; color: var(--text-color); background: var(--bg-color); line-height: 1.6; }
    .bg-video {
      position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -2;
      background: linear-gradient(135deg, #050505 0%, #1a1a1a 100%);
    }
    .bg-video video {
      min-width: 100%; min-height: 100%; width: auto; height: auto;
      position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);
      object-fit: cover; opacity: 0.3;
    }
    .container { max-width: 1100px; margin: 0 auto; padding: 2rem; position: relative; z-index: 1; }
    .wiki-header { margin-bottom: 3rem; border-bottom: 1px solid var(--border-color); padding-bottom: 2rem; }
    .breadcrumb { color: #aaa; margin-bottom: 0.5rem; }
    .breadcrumb a { color: var(--primary-color); text-decoration: none; }
    h1 { font-size: 2.5rem; margin-bottom: 0.5rem; }
    .section { margin-bottom: 4rem; background: var(--card-bg); padding: 2rem; border-radius: 12px; border: 1px solid var(--border-color); }
    h2 { color: var(--primary-color); margin-bottom: 1.5rem; font-size: 1.8rem; border-bottom: 2px solid rgba(255,255,255,0.05); padding-bottom: 0.5rem; }
    h3 { color: #dfe6e9; margin: 1.5rem 0 1rem; }
    p { color: #b2bec3; margin-bottom: 1rem; }
    ul { list-style-position: inside; padding-left: 1rem; }
    .code-block { background: var(--code-bg); border-radius: 8px; border: 1px solid #333; margin: 1rem 0; overflow: hidden; }
    .code-header { background: #252526; padding: 0.5rem 1rem; font-size: 0.8rem; color: #888; display: flex; justify-content: space-between; align-items: center; }
    .copy-btn { background: none; border: none; color: #aaa; cursor: pointer; }
    .copy-btn:hover { color: white; }
    pre { margin: 0; padding: 1rem; overflow-x: auto; }
    .api-table { width: 100%; border-collapse: collapse; margin: 1.5rem 0; font-size: 0.9rem; }
    .api-table th, .api-table td { text-align: left; padding: 1rem; border-bottom: 1px solid rgba(255,255,255,0.1); }
    .api-table th { color: var(--primary-color); background: rgba(0,0,0,0.3); }
    .api-table code { background: rgba(255,255,255,0.1); padding: 2px 5px; border-radius: 4px; color: #fab1a0; font-family: 'Consolas', monospace; }
  </style>
</head>
<body>
  <div class="bg-video">
    <video autoplay muted loop><source src="images/background.mp4" type="video/mp4"></video>
  </div>
  <div class="container">
    <div class="wiki-header">
      <div class="breadcrumb"><a href="index.html"><i class="fas fa-arrow-left"></i> Back to Hub</a> / Wiki</div>
      <h1>Datapack Documentation</h1>
      <p>Create content for Omega without writing code using JSON Datapacks.</p>
    </div>
    <div class="section">
      <h2>File Structure</h2>
      <p>To add custom content, create a Datapack with the following directory structure inside your <code>data/</code> folder.</p>
      <div class="code-block">
        <div class="code-header">Datapack Directory Tree</div>
        <pre><code class="language-bash">data/
  omega/
    omega_clans/              # Custom Clans
      your_clan.json
    omega_clan_specializations/
      your_spec.json
    omega_souls/              # Custom Soul Shards
      your_soul.json
    afo_mappings/             # All For One configurations
      techniques.json</code></pre>
      </div>
    </div>
    <div class="section">
      <h2>JSON Specifications</h2>
      <h3>Clan JSON</h3>
      <p>Place in <code>data/omega/omega_clans/</code>. Ensure the file name matches the ID if possible.</p>
      <div class="code-block">
        <div class="code-header">example_clan.json <button class="copy-btn" onclick="copyCode(this)"><i class="fas fa-copy"></i></button></div>
        <pre><code class="language-json">{
  "id": "mugen",
  "display_name": "Mugen Clan",
  "color": "#00FFFF",
  "has_god_ki": true,
  "specializations": ["Sage", "Cataclysm"],
  "default_traits": ["Angel"], 
  "simple_domain_radius_mult": 1.25,
  "rct_efficiency_mult": 1.5,
  "technique_cost_mult": 0.8,
  "bonus_health": 10.0,
  "bonus_speed": 0.05
}</code></pre>
      </div>
      <h3>Soul JSON</h3>
      <p>Place in <code>data/omega/omega_souls/</code>.</p>
      <div class="code-block">
        <div class="code-header">example_soul.json <button class="copy-btn" onclick="copyCode(this)"><i class="fas fa-copy"></i></button></div>
        <pre><code class="language-json">{
  "id": "ancient_sorcerer",
  "name": "Ancient Sorcerer",
  "color": "#FF0000",
  "blip_sound": "minecraft:entity.experience_orb.pickup",
  "damage_multiplier": 1.2,
  "dialogue": {
    "COMBAT_START": ["Show me your strength.", "I have awoken."],
    "PLAYER_LOW_HEALTH": ["Do not falter now."]
  }
}</code></pre>
      </div>
    </div>
    <div class="section">
      <h2>JSON Attribute Reference</h2>
      <p>This table lists all available keys for use in Clan, Specialization, and Soul JSON files. Not all keys are applicable to every type.</p>
      <table class="api-table">
          <thead>
              <tr>
                  <th>Key</th>
                  <th>Type</th>
                  <th>Default</th>
                  <th>Description</th>
              </tr>
          </thead>
          <tbody>
              <tr><td colspan="4" style="background:rgba(0,0,0,0.2); text-align:center; font-weight:bold;">Identity & Display</td></tr>
              <tr><td><code>id</code></td><td>String</td><td>N/A</td><td>The unique technical ID (e.g., "zenin"). Required.</td></tr>
              <tr><td><code>display_name</code></td><td>String</td><td>ID</td><td>The in-game display name (e.g., "Zenin Clan"). For Souls, this is `name`.</td></tr>
              <tr><td><code>color</code></td><td>String</td><td>"#FFFFFF"</td><td>Hex color code for clan/soul name display.</td></tr>
              <tr><td><code>has_god_ki</code></td><td>Boolean</td><td><code>false</code></td><td>If true, the entity cannot be sensed by Six Eyes unless the viewer also has God Ki.</td></tr>
              <tr><td colspan="4" style="background:rgba(0,0,0,0.2); text-align:center; font-weight:bold;">Clan Features</td></tr>
              <tr><td><code>specializations</code></td><td>List (String)</td><td>[]</td><td>(Clan only) A list of Specialization IDs available to this clan.</td></tr>
              <tr><td><code>default_traits</code></td><td>List (String)</td><td>[]</td><td>(Clan only) A list of Trait IDs granted when this clan is acquired.</td></tr>
              <tr><td><code>default_physical_traits</code></td><td>List (String)</td><td>[]</td><td>(Clan only) A list of Physical Trait IDs granted when this clan is acquired.</td></tr>
              <tr><td colspan="4" style="background:rgba(0,0,0,0.2); text-align:center; font-weight:bold;">Soul Audio & Dialogue</td></tr>
              <tr><td><code>blip_sound</code></td><td>String</td><td>"omega:maleblip"</td><td>(Soul only) Sound event ID for dialogue text.</td></tr>
              <tr><td><code>blip_pitch</code></td><td>Number</td><td>1.0</td><td>(Soul only) Pitch for the blip sound.</td></tr>
              <tr><td><code>blip_volume</code></td><td>Number</td><td>1.0</td><td>(Soul only) Volume for the blip sound.</td></tr>
              <tr><td><code>secondary_blip_sound</code></td><td>String</td><td>null</td><td>(Soul only) Optional second sound for a "fused" voice effect.</td></tr>
              <tr><td><code>dialogue</code></td><td>Object</td><td>null</td><td>(Soul only) An object mapping Dialogue Triggers to lists of strings.</td></tr>
              <tr><td colspan="4" style="background:rgba(0,0,0,0.2); text-align:center; font-weight:bold;">Jujutsu Modifiers</td></tr>
              <tr><td><code>damage_multiplier</code></td><td>Number</td><td>1.0</td><td>Multiplier for unarmed and technique damage.</td></tr>
              <tr><td><code>weapon_damage_multiplier</code></td><td>Number</td><td>1.0</td><td>Multiplier for weapon damage. Stacks with <code>damage_multiplier</code>.</td></tr>
              <tr><td><code>technique_cost_mult</code></td><td>Number</td><td>1.0</td><td>Multiplier for Cursed Energy costs. Lower is better.</td></tr>
              <tr><td><code>domain_expansion_mult</code></td><td>Number</td><td>1.0</td><td>Multiplier for Domain Expansion power/refinement.</td></tr>
              <tr><td><code>simple_domain_radius_mult</code></td><td>Number</td><td>1.0</td><td>Multiplier for Simple Domain's radius.</td></tr>
              <tr><td><code>simple_domain_level_bonus</code></td><td>Number</td><td>0.0</td><td>Flat bonus added to Simple Domain's output level.</td></tr>
              <tr><td><code>da_drain_mult</code></td><td>Number</td><td>1.0</td><td>Multiplier for Domain Amplification's CE drain. Lower is better.</td></tr>
              <tr><td><code>da_level_bonus</code></td><td>Number</td><td>0.0</td><td>Flat bonus added to Domain Amplification's output level.</td></tr>
              <tr><td><code>rct_efficiency_mult</code></td><td>Number</td><td>1.0</td><td>Multiplier for Reverse Cursed Technique healing amount. Higher is better.</td></tr>
              <tr><td><code>rct_fatigue_mult</code></td><td>Number</td><td>1.0</td><td>Multiplier for fatigue gained from using RCT. Lower is better.</td></tr>
              <tr><td><code>guard_damage_mult</code></td><td>Number</td><td>1.0</td><td>Multiplier for damage taken while guarding. Lower is better.</td></tr>
              <tr><td><code>guard_duration_mult</code></td><td>Number</td><td>1.0</td><td>Multiplier for how long a guard can be held.</td></tr>
              <tr><td><code>black_flash_chance_bonus</code></td><td>Number</td><td>0.0</td><td>Additive bonus to Black Flash chance (0.05 = +5%).</td></tr>
              <tr><td colspan="4" style="background:rgba(0,0,0,0.2); text-align:center; font-weight:bold;">Stat Bonuses</td></tr>
              <tr><td><code>bonus_health</code></td><td>Number</td><td>0</td><td>(Clans/Specs) Flat bonus to max health (2 = 1 heart).</td></tr>
              <tr><td><code>health_bonus</code></td><td>Number</td><td>0</td><td>(Souls) Flat bonus to max health (2 = 1 heart).</td></tr>
              <tr><td><code>bonus_armor</code></td><td>Number</td><td>0</td><td>(Clans/Specs) Flat bonus to armor points.</td></tr>
              <tr><td><code>armor_bonus</code></td><td>Number</td><td>0</td><td>(Souls) Flat bonus to armor points.</td></tr>
              <tr><td><code>bonus_attack_damage</code></td><td>Number</td><td>0</td><td>(Clans/Specs) Flat bonus to base attack damage.</td></tr>
              <tr><td><code>attack_bonus</code></td><td>Number</td><td>0</td><td>(Souls) Flat bonus to base attack damage.</td></tr>
              <tr><td><code>bonus_speed</code></td><td>Number</td><td>0.0</td><td>(Clans/Specs) Additive bonus to movement speed (0.1 = +10%).</td></tr>
              <tr><td><code>speed_bonus</code></td><td>Number</td><td>0.0</td><td>(Souls) Additive bonus to movement speed (0.1 = +10%).</td></tr>
              <tr><td><code>bonus_knockback_resistance</code></td><td>Number</td><td>0.0</td><td>(Clans/Specs) Additive bonus to knockback resistance.</td></tr>
          </tbody>
      </table>
    </div>
    <div class="section">
      <h2>Built-in IDs</h2>
      <p>You can use these default IDs in your datapacks, for example, in a clan's <code>"specializations"</code> or <code>"default_traits"</code> lists.</p>
ECHO is off.
      <h3>Traits</h3>
      <ul>
          <li><code>Angel</code></li>
          <li><code>Fallen</code></li>
          <li><code>Deity</code></li>
      </ul>
ECHO is off.
      <h3>Physical Traits</h3>
      <ul>
          <li><code>Vessel</code></li>
      </ul>
ECHO is off.
      <h3>Specializations</h3>
      <ul>
          <li><code>Sage</code></li>
          <li><code>Cataclysm</code></li>
          <li><code>Destroyer</code></li>
          <li><code>Guardian</code></li>
          <li><code>Ascendant</code></li>
          <li><code>Titan</code></li>
      </ul>
    </div>
    <div class="section">
      <h2>Reference Enums</h2>
      <h3>Dialogue Triggers</h3>
      <p>Use these keys within the <code>dialogue</code> object in Soul JSONs to trigger chat messages.</p>
      <table class="api-table">
        <thead><tr><th>Trigger Key</th><th>Description</th></tr></thead>
        <tbody>
          <tr><td><code>COMBAT_START</code></td><td>Fires when the player enters combat (takes damage or deals damage after being idle).</td></tr>
          <tr><td><code>PLAYER_LOW_HEALTH</code></td><td>Fires when player HP drops below 30%.</td></tr>
          <tr><td><code>TARGET_LOW_HEALTH</code></td><td>Fires when the opponent's HP drops below 20%.</td></tr>
          <tr><td><code>PLAYER_KILL_MOB</code></td><td>Fires on killing a standard mob.</td></tr>
          <tr><td><code>PLAYER_KILL_BOSS</code></td><td>Fires on killing a boss (Wither/Ender Dragon).</td></tr>
          <tr><td><code>PLAYER_DEATH</code></td><td>Fires when the player dies.</td></tr>
          <tr><td><code>ENTER_NETHER</code></td><td>Fires when entering the Nether dimension.</td></tr>
          <tr><td><code>ENTER_END</code></td><td>Fires when entering the End dimension.</td></tr>
          <tr><td><code>IDLE</code></td><td>Fires rarely when standing still for a long time.</td></tr>
          <tr><td><code>NIGHT_TIME</code></td><td>Fires occasionally at night.</td></tr>
        </tbody>
      </table>
    </div>
  </div>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.7.0/highlight.min.js"></script>
  <script>
    hljs.highlightAll();
    function copyCode(btn) {
      const pre = btn.parentElement.nextElementSibling;
      navigator.clipboard.writeText(pre.innerText).then(() => {
        btn.innerHTML = '<i class="fas fa-check"></i>';
        setTimeout(() => btn.innerHTML = '<i class="fas fa-copy"></i>', 2000);
      });
    }
  </script>
</body>
</html>

=== SUMMARY ===
HTML: 7
